import Link from"next/link";import type{AssistantMessage as Message}from"@/types/content";export default function AssistantMessage({message,onSuggest}:{message:Message;onSuggest?:(q:string)=>void}){return <div className={`assistantMessage ${message.role}`}><p>{message.content}</p>{message.relatedUrl&&<Link className="assistantRelated" href={message.relatedUrl}>View related page →</Link>}{message.suggestions?.length?<div className="assistantSuggestions">{message.suggestions.map(q=><button key={q} onClick={()=>onSuggest?.(q)}>{q}</button>)}</div>:null}</div>}

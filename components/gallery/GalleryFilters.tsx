export default function GalleryFilters({categories,active,onChange}:{categories:string[];active:string;onChange:(v:string)=>void}){return <div className="filterButtons" aria-label="Gallery categories"><button onClick={()=>onChange("all")} aria-pressed={active==="all"}>All</button>{categories.map(c=><button key={c} onClick={()=>onChange(c)} aria-pressed={active===c}>{c}</button>)}</div>}

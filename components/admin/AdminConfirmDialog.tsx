import{AlertTriangle}from"lucide-react";import AdminModal from"./AdminModal";export default function AdminConfirmDialog({open,title,description,busy,onCancel,onConfirm}:{open:boolean;title:string;description:string;busy?:boolean;onCancel:()=>void;onConfirm:()=>void}){return <AdminModal open={open} title={title} onClose={onCancel} size="medium"><div className="confirmContent"><AlertTriangle size={30}/><p>{description}</p><div className="modalActions"><button type="button" onClick={onCancel}>Cancel</button><button type="button" className="dangerButton" disabled={busy} onClick={onConfirm}>{busy?"Deleting…":"Delete permanently"}</button></div></div></AdminModal>}

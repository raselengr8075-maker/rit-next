import{CheckCircle2,Clock3,Eye,EyeOff,Star}from"lucide-react";export default function AdminBadge({type,label}:{type:string;label?:string}){const value=(label||type).toLowerCase(),Icon=value.includes("featured")?Star:value.includes("published")||value==="active"?CheckCircle2:value.includes("unread")?EyeOff:value.includes("read")?Eye:Clock3;return <span className={`adminBadge badge-${type.toLowerCase().replace(/\s+/g,"-")}`}><Icon size={12} aria-hidden/>{label||type}</span>}

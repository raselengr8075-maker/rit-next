import{Search,X}from"lucide-react";export default function AdminSearch({value,onChange,placeholder="Search content…"}:{value:string;onChange:(v:string)=>void;placeholder?:string}){return <label className="adminSearch"><Search size={17}/><span className="srOnly">Search</span><input value={value} onChange={e=>onChange(e.target.value)} placeholder={placeholder}/>{value&&<button type="button" onClick={()=>onChange("")} aria-label="Clear search"><X size={15}/></button>}</label>}

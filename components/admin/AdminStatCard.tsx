import Link from"next/link";import type{LucideIcon}from"lucide-react";export default function AdminStatCard({label,count,description,href,icon:Icon,detail}:{label:string;count:number;description:string;href:string;icon:LucideIcon;detail?:string}){return <Link className="adminStatCard" href={href}><div className="statIcon"><Icon size={21}/></div><div><span>{label}</span><strong>{count}</strong><small>{detail||description}</small></div></Link>}

'use client';import{useEffect}from"react";import{X}from"lucide-react";export default function AdminModal({open,title,onClose,children,size="large"}:{open:boolean;title:string;onClose:()=>void;children:React.ReactNode;size?:"medium"|"large"}){useEffect(()=>{if(!open)return;const close=(e:KeyboardEvent)=>e.key==="Escape"&&onClose();addEventListener("keydown",close);document.body.style.overflow="hidden";return()=>{removeEventListener("keydown",close);document.body.style.overflow=""}},[open,onClose]);if(!open)return null;return <div className="adminModalBackdrop" role="presentation" onMouseDown={e=>e.target===e.currentTarget&&onClose()}><section className={`adminModal modal-${size}`} role="dialog" aria-modal="true" aria-labelledby="admin-modal-title"><header><h2 id="admin-modal-title">{title}</h2><button type="button" className="iconButton" onClick={onClose} aria-label="Close dialog"><X size={19}/></button></header><div className="adminModalBody">{children}</div></section></div>}

'use client';import{Moon,Sun,Monitor}from"lucide-react";export type AdminTheme="light"|"dark"|"system";export default function AdminThemeToggle({theme,onChange}:{theme:AdminTheme;onChange:(v:AdminTheme)=>void}){const Icon=theme==="light"?Sun:theme==="dark"?Moon:Monitor;return <button className="iconButton" onClick={()=>onChange(theme==="system"?"light":theme==="light"?"dark":"system")} aria-label={`Admin theme: ${theme}. Change theme.`} title={`Theme: ${theme}`}><Icon size={18}/></button>}

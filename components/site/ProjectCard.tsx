import Link from"next/link";import Image from"next/image";import type{Project}from"@/types/content";import BilingualText from"./BilingualText";
export default function ProjectCard({item}:{item:Project}){return <article className="content-card">{item.imageUrl?<Image unoptimized src={item.imageUrl} alt={item.titleEn} width={640} height={420} sizes="(max-width: 720px) 100vw, 33vw"/>:<div className="card-placeholder" aria-hidden="true"><span>RIT</span></div>}<div className="content-card-body"><div className="card-meta"><span>{item.category||"Project"}</span><span className="status-badge">{item.status}</span></div><h3><BilingualText en={item.titleEn} bn={item.titleBn||item.titleEn}/></h3><p><BilingualText en={item.summaryEn} bn={item.summaryBn||item.summaryEn}/></p><Link href={`/projects/${item.slug}`}><BilingualText en="View project" bn="প্রকল্প দেখুন"/> →</Link></div></article>}

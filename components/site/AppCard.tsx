import Link from"next/link";import type{AppProduct}from"@/types/content";import BilingualText from"./BilingualText";export default function AppCard({item}:{item:AppProduct}){return <article className="content-card compact"><div className="app-mark">{item.titleEn.slice(0,2).toUpperCase()}</div><div className="content-card-body"><div className="card-meta"><span>{item.platform||"Digital product"}</span><span className="status-badge">{item.status}</span></div><h3><BilingualText en={item.titleEn} bn={item.titleBn||item.titleEn}/></h3><p><BilingualText en={item.summaryEn} bn={item.summaryBn||item.summaryEn}/></p><Link href={`/apps/${item.slug}`}><BilingualText en="Explore app" bn="অ্যাপ দেখুন"/> →</Link></div></article>}

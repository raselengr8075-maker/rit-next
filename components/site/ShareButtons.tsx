'use client';import{useState}from"react";import{Link2,Share2}from"lucide-react";export default function ShareButtons({title,path}:{title:string;path:string}){const[copied,setCopied]=useState(false);const base=typeof window==='undefined'?(process.env.NEXT_PUBLIC_SITE_URL||''):window.location.origin,url=`${base}${path}`;async function copy(){await navigator.clipboard?.writeText(url);setCopied(true);setTimeout(()=>setCopied(false),1800)}return <div className="share-buttons" aria-label="Share this page"><a href={`https://www.linkedin.com/sharing/share-offsite/?url=${encodeURIComponent(url)}`} target="_blank" rel="noopener noreferrer"><Share2 size={15}/> LinkedIn</a><button onClick={copy}><Link2 size={15}/> {copied?'Copied':'Copy link'}</button><a href={`mailto:?subject=${encodeURIComponent(title)}&body=${encodeURIComponent(url)}`}>Email</a></div>}

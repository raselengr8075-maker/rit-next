import{NextResponse,type NextRequest}from"next/server";import{createServerClient}from"@supabase/ssr";export async function proxy(req:NextRequest){let res=NextResponse.next({request:req});const u=process.env.NEXT_PUBLIC_SUPABASE_URL,k=process.env.NEXT_PUBLIC_SUPABASE_ANON_KEY;if(!u||!k)return res;const db=createServerClient(u,k,{cookies:{getAll:()=>req.cookies.getAll(),setAll(c){c.forEach(x=>req.cookies.set(x.name,x.value));res=NextResponse.next({request:req});c.forEach(x=>res.cookies.set(x.name,x.value,x.options))}}});await db.auth.getUser();return res}export const config={matcher:["/admin/:path*"]};

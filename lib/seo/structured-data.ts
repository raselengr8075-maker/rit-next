const siteUrl = process.env.NEXT_PUBLIC_SITE_URL || "http://localhost:3000";
export const organizationJsonLd = { "@context": "https://schema.org", "@type": "Organization", name: "Rasel Innovation & Technology", alternateName: "RIT", url: siteUrl };
export const websiteJsonLd = { "@context": "https://schema.org", "@type": "WebSite", name: "Rasel Innovation & Technology", url: siteUrl, potentialAction: { "@type": "SearchAction", target: `${siteUrl}/search?q={search_term_string}`, "query-input": "required name=search_term_string" } };
export function breadcrumbsJsonLd(items: { name: string; path: string }[]) { return { "@context": "https://schema.org", "@type": "BreadcrumbList", itemListElement: items.map((item, i) => ({ "@type": "ListItem", position: i + 1, name: item.name, item: new URL(item.path, siteUrl).toString() })) }; }
export function articleJsonLd(input: { title: string; description: string; path: string; published?: string; updated?: string; image?: string; author?: string }) { return { "@context": "https://schema.org", "@type": "Article", headline: input.title, description: input.description, url: new URL(input.path, siteUrl).toString(), ...(input.published ? { datePublished: input.published } : {}), ...(input.updated ? { dateModified: input.updated } : {}), ...(input.image ? { image: input.image } : {}), author: { "@type": "Organization", name: input.author || "Rasel Innovation & Technology" }, publisher: { "@type": "Organization", name: "Rasel Innovation & Technology" } }; }
export function safeJsonLd(value: object) { return JSON.stringify(value).replace(/</g, "\\u003c"); }

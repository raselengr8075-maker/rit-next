import"server-only";import{createServerClient}from"@supabase/ssr";import{cookies}from"next/headers";export async function createClient(){const u=process.env.NEXT_PUBLIC_SUPABASE_URL,k=process.env.NEXT_PUBLIC_SUPABASE_ANON_KEY;if(!u||!k)return null;const s=await cookies();return createServerClient(u,k,{cookies:{getAll:()=>s.getAll(),setAll(v){try{v.forEach(x=>s.set(x.name,x.value,x.options))}catch{}}}})}

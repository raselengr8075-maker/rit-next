import"server-only";import{redirect}from"next/navigation";import{createClient}from"@/lib/supabase/server";import{hasPublicSupabaseConfig}from"@/lib/supabase/config";
export async function requireAdmin(){if(!hasPublicSupabaseConfig())throw new Error("SUPABASE_NOT_CONFIGURED");const db=await createClient();if(!db)throw new Error("SUPABASE_NOT_CONFIGURED");const{data:{user}}=await db.auth.getUser();if(!user)redirect("/admin/login");const{data,error}=await db.from("admin_profiles").select("id").eq("id",user.id).maybeSingle();if(error||!data)redirect("/admin/login?error=unauthorized");return{db,user}}

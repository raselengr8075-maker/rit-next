import{Bot,Database,Globe2,HardDrive,Mail,ServerCog}from"lucide-react";import{hasPublicSupabaseConfig,hasServerSupabaseConfig}from"@/lib/supabase/config";import{requireAdmin}from"@/lib/admin/auth";import{createAdminClient}from"@/lib/supabase/admin";import SetupRequired from"@/components/admin/SetupRequired";import AdminShell from"@/components/admin/AdminShell";import AdminPageHeader from"@/components/admin/AdminPageHeader";import AdminBadge from"@/components/admin/AdminBadge";export default async function Page(){if(!hasPublicSupabaseConfig())return <SetupRequired/>;await requireAdmin();let storage=false;if(hasServerSupabaseConfig()){const{data}=await createAdminClient()!.storage.getBucket("rit-media");storage=Boolean(data)}const settings=[{label:"Supabase connection",value:"Configured",ok:true,detail:"Project URL and anonymous client key are available.",icon:Database},{label:"Server credentials",value:hasServerSupabaseConfig()?"Configured":"Missing",ok:hasServerSupabaseConfig(),detail:"Used only by trusted server workflows.",icon:ServerCog},{label:"Site URL",value:process.env.NEXT_PUBLIC_SITE_URL||"Not enabled",ok:Boolean(process.env.NEXT_PUBLIC_SITE_URL),detail:process.env.NEXT_PUBLIC_SITE_URL||"Set NEXT_PUBLIC_SITE_URL for production canonical URLs.",icon:Globe2},{label:"Storage bucket",value:storage?"Configured":"Not verified",ok:storage,detail:"Expected bucket: rit-media. Verification requires server configuration.",icon:HardDrive},{label:"Email delivery",value:process.env.RESEND_API_KEY&&process.env.CONTACT_TO_EMAIL&&process.env.CONTACT_FROM_EMAIL?"Configured":"Missing",ok:Boolean(process.env.RESEND_API_KEY&&process.env.CONTACT_TO_EMAIL&&process.env.CONTACT_FROM_EMAIL),detail:"Resend contact-form delivery status.",icon:Mail},{label:"Assistant mode",value:"Local",ok:true,detail:"RIT Smart Assistant uses local published knowledge—no external AI API.",icon:Bot}];return <AdminShell><AdminPageHeader title="Settings" description="Safe environment and service status. Secret values are never displayed." icon={ServerCog}/><div className="settingsGrid">{settings.map(({label,value,ok,detail,icon:Icon})=><article className="settingCard" key={label}><span><Icon size={20}/></span><div><div><h2>{label}</h2><AdminBadge type={ok?"active":"inactive"} label={value}/></div><p>{detail}</p></div></article>)}</div><section className="adminPanel environmentNotes"><h2>Environment notes</h2><dl><div><dt>Runtime</dt><dd>{process.env.NODE_ENV}</dd></div><div><dt>Public fallback</dt><dd>Enabled when Supabase is unavailable</dd></div><div><dt>Credential display</dt><dd>Disabled</dd></div><div><dt>Admin authorization</dt><dd>Supabase Auth + admin_profiles</dd></div></dl></section></AdminShell>}
